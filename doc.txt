conda create -p venv python==3.12 -y
conda activate venv/

git init 
create gitignore to avoid commiting venv 
git push -u origin main



pip install -r requirements.txt



{chanege name to PAVAN~1 if error comes when installing packages}